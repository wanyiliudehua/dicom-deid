# DICOM-DeID 使用说明书

> **版本** 1.2.0  
> **功能** DICOM 医学影像脱敏工具（交互式 TUI + 命令行 CLI + REST API）  
> **平台** Windows / macOS / Linux  
> **Python** ≥ 3.11

---

## 一、功能概述

本工具用于 **DICOM 医学影像的去标识化（De-identification）**，保护患者隐私。

### 三种使用方式

| 方式 | 说明 |
|------|------|
| **命令行 CLI** | 一条命令指定「什么功能 + 什么策略」，适合脚本化/批处理（见第十章） |
| **TUI 交互界面** | 终端图形化操作，适合单次手动处理 |
| **REST API** | HTTP 接口，适合批量/自动化集成 |

### 三大核心功能

| 功能 | 说明 |
|------|------|
| **① 审计 (Audit)** | 扫描 DICOM 文件，发现 PHI 字段、私有块内厂商 UID、文件名泄露 |
| **② 脱敏 (Deid)** | 生成脱敏副本（原目录只读），支持 A/B 两档策略 |
| **③ 复验 (Verify)** | 检查脱敏产物是否残留 PHI、像素是否完整 |

### 脱敏策略

| 策略 | 清空内容 | 保留内容 |
|------|----------|----------|
| **A 保守** | 姓名/ID/生日/检查号/机构/设备序列号/医生姓名/流程标识 | 性别/年龄/身高体重/日期时间/设备型号/序列描述 |
| **B 激进** | 全部标识字段 | 仅影像本身临床参数（设备型号/序列描述） |

---

## 二、安装（新机 · 国内环境）

> 以下流程 2026-09-22 在干净 venv 上双路径实测通过（在线清华源 / 离线 wheelhouse）。

### 环境要求

- Python ≥ 3.11（3.11–3.14 均可）
- Windows 10/11、macOS、Linux 均可

### 第 1 步：装 Python（国内镜像）

python.org 国内直连慢，用 npmmirror 镜像（实测可达，含 Windows exe）：

```text
https://registry.npmmirror.com/-/binary/python/3.12.10/python-3.12.10-amd64.exe
```

（版本目录 `3.12.10/` 可换成 3.11.x–3.14.x 任意目录）。Windows 安装时**勾选 "Add python.exe to PATH"**。

### 第 2 步：配置 pip 清华源（一次配置，全局生效）

```bash
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
pip config set global.trusted-host pypi.tuna.tsinghua.edu.cn
```

### 第 3 步：拿到工具文件（二选一）

| 方式 | 操作 |
|---|---|
| 源码目录（推荐，便于改） | 整个拷贝 `D:\Hermes\dicom_deid_app\`（含 `pyproject.toml` + `dicom_deid\`） |
| 离线包（无网机） | 拷贝源码目录 **+** `wheelhouse\` 目录（25 个 whl，含工具本体与全部依赖） |

> **wheelhouse 制作**（有网机上做一次）：
> ```bash
> cd D:\Hermes\dicom_deid_app
> python -m pip download "dicom-deid[api]" -d wheelhouse   # 若未发布到 PyPI 改用下面两步：
> python -m pip wheel . --no-deps -w wheelhouse --no-build-isolation
> python -m pip download "pydicom>=2.4" "numpy>=1.24" "textual>=0.80" "click>=8.0" "rich>=13.0" "fastapi>=0.110" "uvicorn>=0.29" "python-multipart>=0.0.9" -d wheelhouse
> ```
> ⚠ `--no-deps`/`--no-build-isolation` 的 `-w` 路径必须用 **Windows 风格**（`C:/...` 或 `D:/...`）；原生 pip 不认 MSYS 路径（`/c/...`），会静默把 wheel 写到别处。

### 第 4 步：建虚拟环境 + 安装

**路径 A：有网机（清华源在线装）**

```bash
cd D:\Hermes\dicom_deid_app
python -m venv .venv
.venv\Scripts\python -m pip install ".[api]"      # 只要 CLI/TUI 就去掉 [api]
```

**路径 B：无网机（离线 wheelhouse）**

```bash
cd D:\Hermes\dicom_deid_app
python -m venv .venv
.venv\Scripts\python -m pip install --no-index --find-links wheelhouse "dicom-deid[api]"
```

> 无网机若连 Python 都没装：先在有网机下好 `python-3.12.x-amd64.exe` 拷过去装。

**路径 C：系统级安装（不开 venv，图省事）**——把第 4 步的 `python -m pip ...` 前缀去掉直接 `pip install ".[api]"`。

### 第 5 步：验证安装

```bash
.venv\Scripts\python -m dicom_deid --help          # 应显示 {audit,deid,tui,serve}
.venv\Scripts\dicom-deid --help                    # 入口脚本同样可用
.venv\Scripts\python -m dicom_deid audit <DICOM目录> -q   # 真实数据冒烟
.venv\Scripts\python -m dicom_deid serve --port 8080      # 应打印 "Uvicorn running on ..."
```

四个都出正常输出即安装成功。

### 安装踩坑（实测记录）

1. **`serve` 依赖是可选组**：`fastapi/uvicorn/python-multipart` 声明在 `[project.optional-dependencies].api`，**基础安装不含**。漏装 `[api]` 时 `serve` 不再裸抛 `ModuleNotFoundError`，而是提示 `pip install "dicom-deid[api]"`（v1.0.0 起）。
2. **改过 pyproject 后重装要 `--force-reinstall`**：pip 对已满足的 `dicom-deid[api]` 不会重新解析 wheel 元数据，会警告 `does not provide the extra 'api'` 并跳过——增量装依赖时加 `--force-reinstall`。
3. **`build-backend` 曾是错值** `setuptools.backends._legacy:_Backend`（导致 `BackendUnavailable: Cannot import 'setuptools.backends._legacy'`），已改为标准 `setuptools.build_meta`。若你手里的旧拷贝 `pip install` 报这个错，改 pyproject 这一行即可。
4. **原生 pip 不认 MSYS 路径**：`-w /c/...`、`-d /c/...` 会被静默忽略/写错位置，必须用 `C:/...` 形式。
5. **`pip install .`（源码目录）与 `pip install <wheel>` 等价**，都可；日常开发用 `pip install -e .`（可编辑模式，改代码免重装）。

---

## 三、TUI 交互界面

### 启动

```bash
cd D:\Hermes\dicom_deid_app
python run_tui.py
```

### 界面布局

```
┌─────────────────────────────────────────────────────────────────┐
│  DICOM-DeID v1.1     │           ⏳ 运行中   0.0s             │
├────────────┬────────────────────────────────────────────────────┤
│ 工作目录   │  ① 配置  │  ② 日志  │  ③ 结果                    │
│ [D:\DICOM] │                                                 │
│            │  ┌──────────────────────────────────────────────┐ │
│ 操作模式   │  │ 开始审计: 69 个文件                          │ │
│ ● ① 审计   │  │   [1/69] image1.dcm                         │ │
│   ② 脱敏   │  │   [2/69] image2.dcm                         │ │
│   ③ 复验   │  │   ...                                       │ │
│            │  │   审计完成: 69 文件, 0 PHI 标签              │ │
│ 说明       │  └──────────────────────────────────────────────┘ │
│ 扫描 PHI   │                                                 │
│ 字段、私有 │  ┌──────────┬───────────────────┐                │
│ 块内 UID   │  │ 项目     │ 值                │                │
│            │  ├──────────┼───────────────────┤                │
│ 参数       │  │ 文件数   │ 69                │                │
│ 策略: A保守│  │ PHI 标签 │ 20                │                │
│ 重命名: 是 │  │ 文件名泄露│ 0                │                │
│            │  └──────────┴───────────────────┘                │
│ ────────── │                                                 │
│ ▶ Enter    │                                                 │
└─────────────────────────────────────────────────────────────────┘
│ 1/2/3 模式  a A保守  b B激进  r 重命名  Enter 开始  q 退出    │
└─────────────────────────────────────────────────────────────────┘
```

### 快捷键

| 按键 | 功能 |
|------|------|
| `1` | 切换到「审计」模式 |
| `2` | 切换到「脱敏」模式 |
| `3` | 切换到「复验」模式 |
| `a` | 切换到「A 保守」策略 |
| `b` | 切换到「B 激进」策略 |
| `r` | 切换「重命名文件」开关 |
| `Enter` | 执行当前操作 |
| `↑` `↓` | 滚动日志 |
| `Tab` | 切换标签页 |
| `q` / `Esc` | 退出 |

### 操作流程

**审计（推荐先做）：**
1. 在左侧面板输入工作目录（包含 `.dcm` 文件的路径）
2. 按 `1` 切换到「审计」模式
3. 按 `Enter` 开始
4. 查看报告：PHI 标签、私有块 UID、文件名泄露、跨文件一致性

**脱敏：**
1. 输入源 DICOM 目录
2. 按 `2` 切换到「脱敏」模式
3. 选择策略（`a` / `b`）和重命名（`r`）
4. 按 `Enter` 开始
5. 输出到 `源目录/deid_output/`，同时生成 `uid_map.json`

**复验：**
1. 输入脱敏产物目录
2. 按 `3` 切换到「复验」模式
3. 按 `Enter` 开始
4. 查看五项检查结果

---

## 四、REST API 接口

### 启动服务

```bash
cd D:\Hermes\dicom_deid_app
python -m dicom_deid.api
# 或
python run_tui.py --api
```

服务启动后监听 `http://0.0.0.0:8080`。

### 接口列表

#### `GET /health` — 健康检查

```bash
curl http://127.0.0.1:8080/health
```

响应：
```json
{"status": "ok", "version": "1.0.0"}
```

#### `POST /audit` — PHI 审计

```bash
curl -X POST http://127.0.0.1:8080/audit \
  -H "Content-Type: application/json" \
  -d '{"dir": "D:/DICOM/src"}'
```

请求体：
```json
{"dir": "D:/DICOM/src"}
```

响应：
```json
{
  "dir": "D:/DICOM/src",
  "file_count": 69,
  "parse_errors": 0,
  "phi_tags": 20,
  "person_names": 0,
  "private_vendor_uids": 0,
  "filename_leaks": 0,
  "consistency": {
    "study_uids": 1,
    "series_uids": 5,
    "sop_uids": 69
  }
}
```

#### `POST /deid` — 脱敏

```bash
curl -X POST http://127.0.0.1:8080/deid \
  -H "Content-Type: application/json" \
  -d '{"src_dir": "D:/DICOM/src", "profile": "a", "rename_files": true}'
```

请求体：
```json
{
  "src_dir": "D:/DICOM/src",
  "out_dir": "D:/DICOM/out",      // 可选，默认 src_dir/deid_output
  "profile": "a",                  // "a" 保守 / "b" 激进
  "rename_files": true             // 是否重命名为新 SOPInstanceUID
}
```

响应：
```json
{
  "src_files": 69,
  "out_files": 69,
  "tags_cleared": 1342,
  "uids_remapped": 207,
  "csa_uids_replaced": 139,
  "renamed_files": 69,
  "errors": [],
  "output_dir": "D:/DICOM/out"
}
```

#### `POST /verify` — 复验

```bash
curl -X POST http://127.0.0.1:8080/verify \
  -H "Content-Type: application/json" \
  -d '{"dir": "D:/DICOM/out"}'
```

请求体：
```json
{"dir": "D:/DICOM/out"}
```

响应：
```json
{
  "file_count": 69,
  "all_passed": true,
  "filename_hits": 0,
  "content_hits": {},
  "pixel_ok": 0,
  "pixel_bad": 0,
  "study_uids": 1,
  "series_uids": 5,
  "sop_uids": 69
}
```

#### `POST /deid/upload` — 上传文件脱敏

```bash
curl -X POST http://127.0.0.1:8080/deid/upload \
  -F "files=@image1.dcm" \
  -F "files=@image2.dcm" \
  -F "profile=a"
```

响应：
```json
{
  "files": 2,
  "summary": {
    "tags_cleared": 38,
    "uids_remapped": 6,
    "csa_replaced": 4
  }
}
```

---

## 五、Python 编程接口

不使用 TUI 或 API，直接调用模块：

```python
from pathlib import Path
from dicom_deid.policy import Policy, Profile
from dicom_deid.deid import run as deid_run
from dicom_deid.verify import run as verify_run
from dicom_deid.audit import run as audit_run

# 审计
report = audit_run(Path("D:/DICOM/src"))
print(f"文件: {report.file_count}, PHI 标签: {len(report.phi_tags)}")

# 脱敏（A 保守）
policy = Policy(profile=Profile.Conservative, rename_files=True)
summary = deid_run(Path("D:/DICOM/src"), Path("D:/DICOM/out"), policy)
print(f"完成: {summary.out_files} 文件, 清空 {summary.tags_cleared} 标签")

# 复验
result = verify_run(Path("D:/DICOM/out"))
print(f"通过: {result.all_passed}")
```

---

## 六、DICOM 文件处理细节

### 6.1 A 保守策略清空的标签（45 个）

**患者标识：**
`PatientName`, `PatientID`, `PatientBirthDate`, `OtherPatientIDs`, `OtherPatientNames`, `PatientAddress`, `PatientTelephoneNumbers`, `PatientBirthName`, `PatientMotherBirthName`

**检查/机构标识：**
`AccessionNumber`, `StudyID`, `InstitutionName`, `InstitutionAddress`, `InstitutionalDepartmentName`, `InstitutionCodeSequence`, `StationName`, `DeviceSerialNumber`, `StudyDescription`

**人员姓名：**
`ReferringPhysicianName`, `PerformingPhysicianName`, `OperatorsName`, `PhysiciansOfRecord`, `NameOfPhysiciansReadingStudy`, `RequestingPhysician`, `ReferringPhysicianAddress`, `ReferringPhysicianTelephoneNumbers`

**流程标识：**
`PerformedProcedureStepID`, `PerformedProcedureStepDescription`, `PerformedStationName`, `ScheduledProcedureStepID`, `ScheduledPerformingPhysicianName`, `ScheduledStationName`, `ScheduledProcedureStepDescription`, `ScheduledProcedureStepLocation`, `RequestedProcedureID`, `RequestedProcedureDescription`, `CommentsOnThePerformedProcedureStep`, `PlacerOrderNumberImagingServiceRequest`, `FillerOrderNumberImagingServiceRequest`, `AdmissionID`, `CurrentPatientLocation`, `RequestingService`, `OrderCallbackPhoneNumber`, `PersonAddress`, `PersonTelephoneNumbers`

### 6.2 A 保守策略保留的标签

`PatientSex`, `PatientAge`, `PatientSize`, `PatientWeight`, `BodyPartExamined`, `StudyDate`, `StudyTime`, `SeriesDate`, `SeriesTime`, `SeriesDescription`, `Modality`

### 6.3 B 激进策略额外清空的标签

`PatientSex`, `PatientAge`, `PatientSize`, `PatientWeight`, `StudyDate`, `StudyTime`, `SeriesDate`, `SeriesTime`, `ContentDate`, `ContentTime`, `AcquisitionDate`, `AcquisitionTime`, `AcquisitionDateTime`, `InstanceCreationDate`, `InstanceCreationTime`, `InstanceCreationDateTime`

### 6.4 UID 重映射规则

- UID 保持**等长**（旧 UID 长度 = 新 UID 长度）
- 固定根：`1.2.826.0.1.3680043.10.9999.` + SHA256 派生数字
- **绝不修改**：SOPClassUID、TransferSyntaxUID（标准常量）
- CSA 私有块内的 UID 同步替换

### 6.5 Siemens CSA 私有块

- 标签：`(0029,1010)`、`(0029,1020)`
- 内嵌厂商 UID（QCData、tReferenceImage 引用等）
- **子串扫描**（整值比对会漏）
- 替换后**长度不变**，保证二进制结构完整

---

## 七、输出文件说明

脱敏后的目录结构：

```
deid_output/
├── <新SOPInstanceUID1>.dcm    # 脱敏后的 DICOM 文件（已重命名）
├── <新SOPInstanceUID2>.dcm
├── ...
├── uid_map.json               # 旧 UID → 新 UID 映射表（审计追溯）
└── <新StudyInstanceUID>.xml   # 脱敏后的 XML 侧车文件（如果有）
```

`uid_map.json` 示例：
```json
{
  "1.3.12.2.1107.5.2.30.57245.202609192029263830646298": "1.2.826.0.1.3680043.10.9999.70189805974475117002094",
  "1.3.12.2.1107.5.2.30.57245.2026091920292662629246301": "1.2.826.0.1.3680043.10.9999.204009338588192113517763"
}
```

---

## 八、项目结构

```
dicom_deid_app/
├── dicom_deid/
│   ├── __init__.py       # 版本信息
│   ├── policy.py         # A/B 脱敏策略定义
│   ├── uid_map.py        # UID 长度保持重映射
│   ├── csa.py            # Siemens CSA 私有块处理
│   ├── audit.py          # PHI 审计（只读扫描）
│   ├── deid.py           # 脱敏主流程
│   ├── verify.py         # 复验（残留 PHI 检查）
│   ├── tui.py            # Textual TUI 交互界面
│   └── api.py            # FastAPI REST 接口
│   ├── cli.py            # 命令行入口（audit/deid/tui/serve）
│   └── __main__.py       # python -m dicom_deid 入口
├── run_tui.py            # 启动入口（TUI / API 模式）
├── pyproject.toml        # 项目配置
└── 使用说明书.md          # 本文档
```

---

## 九、注意事项

1. **源目录只读** —— 脱敏过程不会修改源文件，所有输出到副本目录
2. **不可逆操作** —— 脱敏后的文件无法恢复原始数据，建议先做好备份
3. **A 方案保留信息** —— A 保守策略保留性别/年龄/身高/体重/日期，仍可能被反推身份
4. **B 方案更彻底** —— B 激进策略清除全部标识字段
5. **大文件处理** —— 69 个 DICOM 文件约需 5-15 秒完成脱敏
6. **API 并发** —— API 服务使用线程池，默认 4 个 worker

---

## 十、命令行（CLI）使用

**核心设计：功能 = 子命令，策略 = `-p` 参数（脱敏时必选）。**

```bash
cd D:\Hermes\dicom_deid_app

# 无参数 = 交互向导：① 选功能（审计/脱敏）→ ② 选策略（A 保守 / B 激进）→ ③ 填路径
python -m dicom_deid

# ① 审计（只读，与策略无关）
python -m dicom_deid audit <DICOM目录> [--json 报告.json] [--needle 特征串] [-q]

> **审计判定规则**：PHI 标签**存在但值为空**（脱敏产物的 `PatientName=""`）不计为发现；
> 只有**非空值**才算 PHI。因此「脱敏输出 → 重审计」应得 `结论：未发现 PHI` / 退出码 0。
> 反之，目录中若有**未检查的非 DICOM 文件**，即使已扫文件全洁净也返回退出码 1（见「文件识别」）。

# ② 脱敏（策略必须显式指定：a/conservative/保守 或 b/aggressive/激进）
python -m dicom_deid deid <源目录> <输出目录> -p a --dry-run   # 试运行，不写文件
python -m dicom_deid deid <源目录> <输出目录> -p b --yes        # 真正执行

# 附：TUI / API
python -m dicom_deid tui
python -m dicom_deid serve --port 8080
```

安装为系统命令（`pip install -e .` 后）可直接用 `dicom-deid` 代替 `python -m dicom_deid`。

### deid 子命令参数

| 参数 | 说明 |
|------|------|
| `-p, --profile` | **必选**。`a`/`保守` = A 保守；`b`/`激进` = B 激进 |
| `--dry-run` | 试运行：走完全流程但不写任何文件（推荐先跑一次看统计） |
| `-y, --yes` | 跳过确认（脚本化调用必须显式加，非交互环境不加会直接中止） |
| `--keep-filename` | 不按新 SOPInstanceUID 重命名（⚠ 原文件名可能含设备序列号） |
| `--force` | 允许写入已含 `.dcm` 的输出目录（默认拒绝，防止新旧文件混叠） |
| `--json FILE` | 摘要另存为 JSON |

### 文件识别（v1.2.1+）

> **申明：本工具仅支持 DICOM 原图。** 裸图像格式（`.jp2` / `.png` / `.jpg` 等）不是 DICOM，
> 无元数据可脱敏，不在本工具能力范围内；**图像像素内烧录的文字（如报告单截图）更无法通过元数据脱敏消除**，
> 需另行做文件名改名 / 像素级遮盖处理。

- **不依赖扩展名**：`.dcm` / `.dicom` / `.img` / 无扩展名 / 无 128 字节前导 均可识别（魔数嗅探 + 试探解析兜底）
- **无损压缩 DICOM 直接支持**：RLE（1.2.840.10008.1.2.5）、JPEG2000 无损（...1.2.4.90）、JPEG-LS 等封装压缩格式——像素字节**透传**、传输语法保持不变，**无需安装任何解码插件**
- **非 DICOM 文件**：明确警告并跳过，本工具不改动它们

### 安全护栏

1. **源目录只读**，输出到副本目录
2. **输出目录 = 源目录 → 直接拒绝**（覆盖原数据是独立高危流程，不在本命令内做）
3. 执行前打印「清空什么 / 保留什么」的完整计划，交互下需确认
4. 非交互环境（管道/CI）不加 `--yes` 一律中止，不写任何文件

### 退出码（供脚本判断）

| 退出码 | 含义 |
|--------|------|
| 0 | 成功 / 洁净 |
| 1 | 完成但有发现（审计查到 PHI、存在未检查的非 DICOM 文件、脱敏有错误） |
| 2 | 出错或被护栏中止 |

> A 保守策略保留 性别+年龄+检查日期，小范围内仍可能被反推身份；**公开发布数据集请用 `-p b`**。脱敏 ≠ 可随意外发。

---

## 十一、常见问题

**Q: TUI 启动后黑屏？**  
A: 确认终端支持 ANSI 转义序列。Windows 下使用 Windows Terminal 或 Git Bash。

**Q: 脱敏后文件打不开？**  
A: 检查 SOPClassUID 和 TransferSyntaxUID 是否被意外修改。本工具会保留这两个标准常量。

**Q: 如何处理 XML 侧车文件？**  
A: 脱敏时会自动处理同目录下的 `.xml` 文件，按 tag 号清空 PHI 标签。

**Q: 复验显示"文件名泄露"？**  
A: 说明文件名中仍含原厂 UID（含设备序列号），需要启用「重命名文件」功能后重新脱敏。

**Q: API 如何处理大文件？**  
A: 使用 `/deid/upload` 接口上传文件，或先将文件放到服务器目录，再调用 `/deid` 接口。

**Q: 如何集成到 HIS/PACS 系统？**  
A: 启动 API 服务后，通过 HTTP 调用 `/audit` → `/deid` → `/verify` 完成流水线。

---

*文档版本：1.2.0 | 更新日期：2026-09-22*
